{% set unbound_config = salt['pillar.get']('node:unbound') %}
{%- do salt.log.error(unbound_config) -%}
{% for domain in unbound_config.get('domainoverrides', {}) %}
{%- do salt.log.error(domain) -%}
forward-zone:
        name: {{ domain }}
        forward-addr: {{ ip }}
{% endfor %}