{%- set ssh_config = salt['pillar.get']('ssh') %}
{%- set node_config = salt['pillar.get']('node') -%}
{%- set auth_keys = salt['bngs_auth.get_ssh_authkeys'](ssh_config, node_config, grains['id'], username) -%}
{{ "\n".join (auth_keys) }}